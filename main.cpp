#include "pico/stdlib.h"
#include "Constants.h"

int main(){

    gpio_init(SERVO_1_PIN);
    gpio_init(SERVO_2_PIN);
    gpio_init(SERVO_3_PIN);
    gpio_init(SERVO_4_PIN);
    gpio_init(SERVO_5_PIN);
    gpio_init(SERVO_6_PIN);
    gpio_init(SERVO_7_PIN);
    gpio_init(SERVO_8_PIN);
    gpio_init(SERVO_9_PIN);
    gpio_init(SERVO_10_PIN);
    gpio_init(SERVO_11_PIN);
    gpio_init(SERVO_12_PIN);
    gpio_init(SERVO_13_PIN);
    gpio_init(SERVO_14_PIN);
    gpio_init(SERVO_15_PIN);
    gpio_init(SERVO_16_PIN);
    gpio_init(SERVO_17_PIN);
    gpio_init(SERVO_18_PIN);

    

}